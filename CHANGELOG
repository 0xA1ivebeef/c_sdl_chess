
--------------------------------------------------------------------------------

03-03-26
major bugs with mutation of Position Struct and random king square updates 
comming from undo move, in this case beeing called by filter_moves

on removing filter moves: bugs disappear for now, enpassant square is not set
on double pawn push though

enpassant is fixed and works for both sides as tested but not with perft

attack bitboards now include enpassant correctly

castling is added back to the game and works as tested but not with perft

enpassant is not uint8_t instead of int 

--------------------------------------------------------------------------------

04-03-2026
adjusted attack_generator so that both players attack bitboards can be generated 
given to the function as a parameter, enpassant for both players tested

added is_check back to the game

removed king_sq from Position struct 

added filter moves back to the game but it needs testing and refining

--------------------------------------------------------------------------------

05-03-2026
fixing make move and undo move, reducing undo move to captured piece enpassant and
castling the rest is implicit through move and position 

fixed order of apply move and undo move to match inversion and added moved_piece
to undo move to make it more stable and reduce the risk for bugs

fixed update_castle_rights, removed responsibility from apply castling

fully reintroducing filter_moves causes some weird stuff
back double pawn pushes that move into actual enpassant are not possible
the pawn is just moved back, enpassant is still possible but doesnt capture
the pawn it adds it to the board for some reason 
-> phantom pawn

!!! i can put my own king in check

--------------------------------------------------------------------------------

06-03-2026
moved special move handler functions into move_handler
and fixed logic in them and combined with undo logic

implemented a memcpy of backup and position for apply and undo move to make sure
undo doesnt mutate the position which it does for enpassant thats why this causes
undefined behaviour

--------------------------------------------------------------------------------

07-03-2026
FIXED all enpassant bugs, now works properly undo does not mutate the position anymore
TODO promotion pieces are not as chosen (knight promotion always)

fixed pawn promotions so the selection is right again

render legal moves looks nicer now, not just coloing the destsquare red
drawing a small circle for normal moves and a large ring for captures

--------------------------------------------------------------------------------

08-03-2026
reintroducing perft and running tests on different positions
perft is one move off at depth 3 a2a4 421 instead of 420
enpassant bug wraparound was causing this
at depth 5 it reappears double pawn pushes are one move off
todo fix enpassant logic in move_generator

--------------------------------------------------------------------------------

09-03-2026
all perft results from https://www.chessprogramming.org/Perft_Results are met
engine is in a "correct" state 
replaced bitboard resolving with __builtin_ctzll

--------------------------------------------------------------------------------

10-03-2026
added small opponent and material based search algorithm
only works on depth 3 and fails if its in check (finds the right move but fails to play it)

--------------------------------------------------------------------------------

11-03-2026
chages to the gameloop from pollevent to waiteventtimeout because searches
which switch the player internally mess with the logic also added a aimovepending
flag just to make sure

the issue was the search mutated the legal moves so the opponent picked a legal move
but the engine had mutated leagal moves so just generating moves again after the
search fixes this

sort moves by descending order of MVV-LVA value in move_generator

zobrist hashing for implementing a openings book in the future and transposition table maybe
implemented incremental updating of zobrist in apply_move

save_state is now implicit to apply move and doesnt have to be called before it

full incremental zobrist hashing with full special move handling

--------------------------------------------------------------------------------

